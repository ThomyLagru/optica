// let desc = prompt("Ingrese el codigo de descuento");

function sumar(numeroA,numeroB) {
    let resultado = numeroA+numeroB;
    return resultado;
}

function mostrar(mensaje) {
    console.log(mensaje);
}


if (resultado > 1000) {
    function descuento(resultado,desc1) {
        let precio2 = resultado*desc1;
        return precio2;
    }
}

const desc1 = parseInt(0.10)
let numeroA = parseInt(prompt ("Ingresa el precio del producto"));
let numeroB = parseInt(prompt ("Ingresa el precio del otro producto"));


mostrar(sumar(numeroA, numeroB));
mostrar(descuento(resultado, desc1));


/////////////////////////////////////////

// let desc = prompt("Ingrese el codigo de descuento");

